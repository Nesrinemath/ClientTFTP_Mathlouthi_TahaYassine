
#include <stdio.h>
#include <stdlib.h>

/*int main(int argc, char **argv) {
    // Check if the correct number of command-line arguments is provided
    if (argc != 4) {
        // Display an error message and exit the program with success status
        printf("Wrong usage: puttftp filename host port\n");
        exit(EXIT_SUCCESS);
    }
    // Extract command-line arguments
    const char *filename = argv[1];
    const char *host = argv[2];
    const char *port = argv[3];

    // Display information about the puttftp server
    printf("puttftp Server: Filename: %s, Host: %s, Port: %s \n", filename, host, port);

    // Return 0 to indicate successful execution
    return 0;
}*/